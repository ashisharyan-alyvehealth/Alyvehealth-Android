<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:layout_height="wrap_content"  android:layout_width="wrap_content">

    <!-- Background color -->
    <solid android:color="@color/colorCoinBack" />

    <!-- Stroke around the background, width and color -->
    <stroke android:width="4dp" android:color="@color/colorCoinBack"/>

    <!-- The corners of the shape -->
    <corners android:radius="40dp"/>

    <!-- Padding for the background, e.g the Text inside a TextView will be 
    located differently -->
    <padding android:left="10dp" android:right="10dp"
        android:bottom="10dp" android:top="10dp" />

</shape>
